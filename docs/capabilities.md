# Capabilities

AxisCanon is designed as an API primitive for agents and services.

## Contract principles
- Deterministic normalization output
- Atomic request/response processing
- Bounded payload/runtime/fallback
- Machine-readable limits and pricing

Use `/capabilities` for current limits/features and `/manifest` for discoverability metadata.
